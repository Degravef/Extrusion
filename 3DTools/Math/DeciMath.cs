namespace _3DTools.Math;

public static class DeciMath
{
	//TODO precision
	public static decimal Sin(decimal angle) => (decimal) System.Math.Sin((double) angle);
	//TODO precision
	public static decimal Cos(decimal angle) => (decimal) System.Math.Cos((double) angle);
}
